<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uottawa.jasonsmith.homeservicesapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogInPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationInfo" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/roundbutton" />

        <activity android:name=".CreateNewAccount" />
        <activity android:name=".WelcomeScreen" />
        <activity android:name=".admin_interface" />
        <activity android:name=".activity_service_provider_interface" />
        <activity android:name=".activity_SP_information"></activity>
    </application>

</manifest>